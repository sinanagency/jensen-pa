node_modules
.next
.vercel
.env
.env*.local
*.tsbuildinfo
*.log
.DS_Store

# Local doctrine + agent worktrees (kept untracked — see CLAUDE.md)
.claude/worktrees/

# OpenAI export raw — too large for git, lives locally
specs/001-export-mining/raw/
__pycache__/
*.pyc
supabase/.temp/

# Vercel CLI env dumps - NEVER commit (added 2026-06-12 after leak)
.env.vercel*
.env.production
*.env
.env.test*


# secret guard: any file matching these patterns is suspicious by default
.env.test.tmp
*credentials*
*.pem
*.key
*.p12
*.pfx
.env.prod
